extends layout

block content
  h1= title

  form(method='POST', action='')
    div.form-group
      label(for='game') Game:
      select#game.form-control(type='select' name='game' required, placeholder='Select Game')
        each game in game_list
          option(value=game._id) #{game.title}
    div.form-group
      label(for='isbn') ISBN:
        input#isbn.form-control(type='text' name='isbn' required placeholder='Enter ISBN')

    div.form-group
      label(for='status') Status:
      select#status.form-control(type='select' name='status' required, placeholder='Select Availability')
        option(value='Available') Available
        option(value='Reserved') Reserved
        option(value='Loaned') Loaned
        option(value='Unreleased') Unreleased

    div.form-group
      label(for='due_back') Date when game available:
      input#due_back.form-control(type='date' name='due_back' value=(undefined===gameinstance? '' : gameinstance.dueBack))

    button.btn.btn-primary(type='submit') Submit

    if errors
      ul
        for error in errors
          li!= error.msg
